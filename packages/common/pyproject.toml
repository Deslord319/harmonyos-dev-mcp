[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "harmonyos-mcp-common"
version = "0.3.0"
requires-python = ">=3.10"
dependencies = ["loguru>=0.7.0", "typing-extensions>=4.8.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
