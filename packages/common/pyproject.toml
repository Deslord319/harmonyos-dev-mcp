[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "harmonyos-mcp-common"
version = "0.6.0"
requires-python = ">=3.10"
dependencies = ["loguru>=0.7.0", "typing-extensions>=4.8.0", "fastmcp>=0.1.0"]

[tool.hatch.build.targets.wheel]
packages = ["src/common"]
